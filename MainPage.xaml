<Page
    x:Class="UWPFeetInches.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0"
                    Orientation="Horizontal">
            <TextBlock Text="Enter Inches Value:"
                       Margin="0,0,10,0"
                       VerticalAlignment="Center" />
            <TextBox Text="{x:Bind ViewModel.Inches, Mode=TwoWay, Converter={StaticResource DecimalConverter}}" />
        </StackPanel>
        
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal">
            <TextBlock Margin="0,0,10,0"
                       Text="Inches:"/>
            <TextBlock Text="{x:Bind ViewModel.Inches, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</Page>
